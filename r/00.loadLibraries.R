################################################################################
# load libraries

library(rpart)
library(rpart.plot)

library(arules)
library(arulesViz)

library(ggplot2)

library(FSelector)

library(rattle)
